# Alpaca-52K dataset configuration

data:
  name: tatsu-lab/alpaca
  split: train
  num_samples: null
  validation_split: null  # No built-in validation split
  max_eval_samples: 500
  prompt_template: alpaca
  response_only_loss: true
  shuffle_seed: 42
